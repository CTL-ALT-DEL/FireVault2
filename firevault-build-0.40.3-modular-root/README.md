# FireVault Build 0.40.3 — Compact Settings Alignment Pass

Root-level package for GitHub Pages.

## Changes
- Replaced the oversized Settings hero box with a compact topbar.
- Reduced the visual weight of the Diagnostics button so it no longer dominates the page.
- Reformatted Settings submenus with smaller fonts, tighter spacing, and corrected left alignment.
- Tightened settings cards, labels, input fields, checkbox rows, preset buttons, and save bars.
- Improved mobile tab sizing so Settings uses less vertical room on iPhone/PWA installs.
- Kept the selected #8 Flame Icon as the app header mark, favicon, Apple touch icon, and PWA icon.
- Preserves the existing FireVault localStorage key from the modular line: `firevault_vault_build_030`.

Suggested commit:

`Build 0.40.3 compact settings alignment pass`

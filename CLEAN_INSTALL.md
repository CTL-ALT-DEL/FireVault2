# FireVault Build 0.79.8

Deploy the contents of this package over the existing GitHub Pages repository. Do not delete the installed iPhone Home Screen app and do not clear Safari website data.

The existing FireVault vault key remains `firevault_vault_build_030`. This release removes Accounts filter tiles and extra Nearby GPS summary panels; it does not migrate or replace account data.

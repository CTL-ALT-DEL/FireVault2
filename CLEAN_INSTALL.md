# Clean Install Notes — FireVault Build 0.50.43

This ZIP is intended to be used as the new clean baseline for FireVault Build 0.50.43.

## Install

1. Extract the ZIP.
2. Upload the extracted root contents to the GitHub Pages branch/root.
3. Confirm `index.html`, `manifest.json`, `src/`, and `assets/` are at the web root.
4. Hard refresh the browser or clear the PWA cache if needed.

## Build focus

Build 0.50.43 keeps the stable splash/header repair and Home spacing polish, then adds a Report Center **Copy Action Items** tool.

## Notes

- Uses the stable 0.50.42 baseline.
- Keeps the 5-second splash screen.
- Keeps the fixed top header behavior.
- Does not reintroduce job-status workflow buttons.

# FireVault Build 0.94.7 deployment

Deploy all Build 0.94.7 files together. Existing FireVault records, technician profile details, Photo Overlay settings, IndexedDB media, and the `firevault_vault_build_030` storage key remain compatible. Refresh the installed PWA after deployment so the new service worker activates.

# FireVault Build 0.79.5

Deploy the contents of this package over the existing GitHub Pages repository. Do not delete the installed iPhone Home Screen app and do not clear Safari website data.

The existing FireVault vault key remains `firevault_vault_build_030`. Microsoft connection profiles and public app-registration settings are stored separately on the device and are excluded from FireVault vault exports. No Microsoft passwords or OAuth tokens are stored.

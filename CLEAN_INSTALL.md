# FireVault Build 0.79.6

Deploy the contents of this package over the existing GitHub Pages repository. Do not delete the installed iPhone Home Screen app and do not clear Safari website data.

The existing FireVault vault key remains `firevault_vault_build_030`. This release changes only the Accounts directory scrolling behavior and does not migrate or replace account data.

# Clean Install — Build 0.87.5

Deploy the complete package so the updated HTML, JavaScript, CSS, service worker, manifest, and version metadata remain synchronized. Existing FireVault customer data is preserved because the real-vault storage key is unchanged. Demo Mode uses the existing protected fictional dataset and does not overwrite the real vault.

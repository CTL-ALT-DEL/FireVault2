# Clean Install Notes — FireVault Build 0.42.0

Use this ZIP as a clean root package.

Copy the contents into your app project root. Do not copy the outer `firevault-build-*` folder into your repository.

Before replacing files, export a FireVault backup from Settings → Backup.

This build keeps the same storage key, so existing local FireVault data should remain compatible.

# Clean Install — Build 0.87.6

Deploy the complete package so the updated JavaScript, CSS, HTML, service worker, manifest, and version metadata remain synchronized. Existing FireVault customer data and Demo Mode data are preserved because the storage keys are unchanged. After deployment, fully close and reopen the installed PWA so the new service-worker cache is activated.

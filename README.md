# FireVault Build 0.41.0 — Service Call Quick Events

This build continues the modular FireVault roadmap from 0.40.9.

## Updated

- Advanced visible build number to **0.41.0**.
- Added quick-event chips to Service Call Mode for common field notes.
- Reworked Job Mode into a cleaner live service-call screen with timer, quick actions, custom event capture, and event timeline.
- Finished visits now save a summary, start/end timestamps, duration, and event list.
- Site detail now shows a compact Recent Visits card from saved service calls.
- Generated reports now include latest saved visit history.
- Kept the minimalist Settings pill-tab picker from 0.40.9.
- Kept the selected #8 Flame Icon logo and cache-busted app assets.
- Preserved the modular storage key: `firevault_vault_build_030`.
- Kept the clean root package format with no nested build folders.

## Clean install reminder

Delete any old extracted folders named `firevault-build-*` from your local project root before copying this build into place.

Suggested commit message:

`Build 0.41.0 service call quick events`
